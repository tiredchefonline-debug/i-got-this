export default async (req, context) => { const { searchParams } = new URL(req.url); const target = searchParams.get("url"); if (!target) return new Response(JSON.stringify({ error: "Missing url" }), { status: 400 }); const result = { url: target, title: "Looks ok", meta: true, favicon: true, tips: ["Add alt text", "Set image sizes to reduce CLS"] }; return Response.json(result); };